{% extends '::base.html.twig' %}

{% block title %}{{ benchmark.name }} | {{ parent() }}{% endblock %}

{% block body %}

    <div>
        <h3>{{ benchmark.name }}</h3>
        <p>{{ benchmark.description }}</p>
    </div>
    <hr>
    <h4>Заполните поля таблицы значениями:</h4>
    <form autocomplete="off" action="{{ path("fillingBench", {'id' : benchmark.id}) }}" method="post">

        <table class="table table-bordered table-condensed">
            {% for row in benchmark.rows %}
                {#<tr>#}
                {#{% for col in table.columns %}#}
                {#<td><input name="value[{{ row.id }}][{{ col.id }}]" value="{{  }}" ></td>#}
                {#{% endfor %}#}
                {#</tr>#}
                <tr>
                    {% for col in benchmark.columns %}
                        <td><input name="value[{{ row.id }}][{{ col.id }}]" value="" ></td>
                    {% endfor %}
                </tr>
            {% endfor %}
        </table><br>

        <button type="submit" class="btn btn-success">Создать</button>

    </form>
    <a href="{{ path('userPage',{'id':benchmark.user.id}) }}" class="btn">Назад</a>

{% endblock %}